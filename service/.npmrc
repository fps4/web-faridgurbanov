# Route the @fps4 scope to GitHub Packages (published name)
@fps4:registry=https://npm.pkg.github.com

# Use an environment variable for auth; do not commit tokens
//npm.pkg.github.com/:_authToken=${GITHUB_PACKAGES_TOKEN}
always-auth=true
